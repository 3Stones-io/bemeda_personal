---
description: 
globs: 
alwaysApply: true
---

- Always run `mix format && mix prettier` before running `make check_code`
- Elixir formatter issues are fixed automatically by running `mix format`
- Prettier issues are fixed by `mix prettier`
